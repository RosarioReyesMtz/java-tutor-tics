
digraph problema_1_5 {
    // Configuración general
    rankdir=TB;
    node [fontname="Arial", fontsize=10];
    
    // Inicio
    inicio [label="INICIO", shape=ellipse, style=filled, fillcolor="#90EE90"];
    
    // Entrada de datos
    leer [label="Ingresar:\nRadio\nAltura", shape=parallelogram, style=filled, fillcolor="#ADD8E6"];
    
    // Proceso: Calcular Volumen
    calc_vol [label="Calcular Volumen:\nVolumen = π × Radio² × Altura", shape=box, style=rounded];
    
    // Proceso: Calcular Área
    calc_area [label="Calcular Área Lateral:\nÁrea = 2 × π × Radio × Altura", shape=box, style=rounded];
    
    // Salida de resultados
    salida [label="Resultados:\nVolumen\nÁrea", shape=egg, style=filled, fillcolor="#FFB6C1"];
    
    // Fin
    fin [label="FIN", shape=ellipse, style=filled, fillcolor="#FF6B6B"];
    
    // Conexiones
    inicio -> leer;
    leer -> calc_vol;
    calc_vol -> calc_area;
    calc_area -> salida;
    salida -> fin;
}