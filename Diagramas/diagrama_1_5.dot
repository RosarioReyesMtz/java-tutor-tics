digraph problema_1_5 {
    // Configuración general
    rankdir=TB;
    node [fontname="Arial", fontsize=11];
    
    // Inicio
    inicio [label="INICIO", shape=ellipse, style=filled, fillcolor="#90EE90"];
    
    // Entrada de datos
    entrada [label="Leer:\nRADIO (radio del cilindro)\nALTURA (altura del cilindro)", shape=parallelogram, style=filled, fillcolor="#87CEEB"];
    
    // Cálculo del volumen
    calc_vol [label="VOL = π × RADIO² × ALTURA\n(Calcular volumen)", shape=box];
    
    // Cálculo del área lateral
    calc_area [label="AREA = 2 × π × RADIO × ALTURA\n(Calcular área lateral)", shape=box];
    
    // Salida de resultados
    salida [label="Imprimir:\n\"Volumen: \", VOL\n\"Área Lateral: \", AREA", shape=egg, style=filled, fillcolor="#FFB6C1"];
    
    // Fin
    fin [label="FIN", shape=ellipse, style=filled, fillcolor="#FF6B6B"];
    
    // Conexiones
    inicio -> entrada;
    entrada -> calc_vol;
    calc_vol -> calc_area;
    calc_area -> salida;
    salida -> fin;
}